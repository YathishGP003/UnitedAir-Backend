<?xml version="1.0" encoding="UTF-8"?>
<!--
    UnitedAir AI - Smart Flight Booking Assistant
    Backend build definition.

    Version pins mirror the reference environment documented in config.md.txt.
    Two deliberate deviations are recorded in docs/12-TROUBLESHOOTING.md:

      1. Apache Tika is pulled in as targeted parser modules (PDF / MS Office /
         text) rather than tika-parsers-standard-package. Only PDF, DOCX and TXT
         are accepted for ingestion, and the standard package drags ~50 extra
         jars with known Jackson and commons-logging conflicts against Spring
         Boot 3.5.

      2. Testcontainers is omitted because it requires a Docker runtime, which
         this deployment explicitly does not use. Integration tests run against
         the local MariaDB instance that bootstrap.ps1 provisions.
-->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.5.16</version>
        <relativePath/>
    </parent>

    <groupId>com.unitedair</groupId>
    <artifactId>unitedair-ai</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <name>unitedair-ai</name>
    <description>UnitedAir AI - Agentic RAG Smart Flight Booking Assistant</description>

    <properties>
        <java.version>21</java.version>
        <spring-ai.version>1.1.8</spring-ai.version>
        <springdoc.version>2.8.17</springdoc.version>
        <tika.version>3.3.1</tika.version>
        <mockito.version>5.17.0</mockito.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.springframework.ai</groupId>
                <artifactId>spring-ai-bom</artifactId>
                <version>${spring-ai.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <!-- ============================================ Spring Boot core -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-jdbc</artifactId>
        </dependency>

        <!-- ================================================== Security -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>
        <!-- Provides Nimbus JwtEncoder / JwtDecoder used for HS256 tokens -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-oauth2-resource-server</artifactId>
        </dependency>

        <!-- =================================================== Spring AI -->
        <!--
            OpenAI-compatible client. Points at GitHub Models by default; any
            OpenAI-compatible endpoint works by changing LLM_BASE_URL.

            Note: the MariaDB vector-store starter is intentionally not used.
            Retrieval is implemented directly over a single kb_chunk table so
            that the vector lane and the FULLTEXT lexical lane read identical
            rows, and so metadata filters can be pushed into SQL and applied
            BEFORE similarity scoring, as SRS 4.3.2 requires.
        -->
        <dependency>
            <groupId>org.springframework.ai</groupId>
            <artifactId>spring-ai-starter-model-openai</artifactId>
        </dependency>

        <!-- ==================================================== Database -->
        <dependency>
            <groupId>org.mariadb.jdbc</groupId>
            <artifactId>mariadb-java-client</artifactId>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-core</artifactId>
        </dependency>
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-mysql</artifactId>
        </dependency>

        <!-- ========================================= Document extraction -->
        <dependency>
            <groupId>org.apache.tika</groupId>
            <artifactId>tika-core</artifactId>
            <version>${tika.version}</version>
        </dependency>
        <dependency>
            <groupId>org.apache.tika</groupId>
            <artifactId>tika-parser-microsoft-module</artifactId>
            <version>${tika.version}</version>
            <exclusions>
                <!-- The 14 MB "full" OOXML schema set is only required for
                     uncommon vendor extensions. The lite schemas cover the
                     DOCX ingestion formats accepted by this application. -->
                <exclusion>
                    <groupId>org.apache.poi</groupId>
                    <artifactId>poi-ooxml-full</artifactId>
                </exclusion>
                <!-- Bouncy Castle is pulled in for encrypted Outlook/email
                     formats, which this application does not accept. Plain
                     PDF and DOCX ingestion do not require it. -->
                <exclusion>
                    <groupId>org.bouncycastle</groupId>
                    <artifactId>bcjmail-jdk18on</artifactId>
                </exclusion>
                <exclusion>
                    <groupId>org.bouncycastle</groupId>
                    <artifactId>bcprov-jdk18on</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
        <dependency>
            <groupId>org.apache.poi</groupId>
            <artifactId>poi-ooxml-lite</artifactId>
            <version>5.5.1</version>
        </dependency>
        <dependency>
            <groupId>org.apache.tika</groupId>
            <artifactId>tika-parser-pdf-module</artifactId>
            <version>${tika.version}</version>
            <exclusions>
                <!-- Encrypted/signed PDFs are outside the accepted ingestion
                     contract; ordinary policy PDFs use PDFBox without BC. -->
                <exclusion>
                    <groupId>org.bouncycastle</groupId>
                    <artifactId>bcjmail-jdk18on</artifactId>
                </exclusion>
                <exclusion>
                    <groupId>org.bouncycastle</groupId>
                    <artifactId>bcprov-jdk18on</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
        <!-- Direct because passenger e-tickets and receipts are first-class output. -->
        <dependency>
            <groupId>org.apache.pdfbox</groupId>
            <artifactId>pdfbox</artifactId>
            <version>3.0.7</version>
        </dependency>
        <dependency>
            <groupId>org.apache.tika</groupId>
            <artifactId>tika-parser-text-module</artifactId>
            <version>${tika.version}</version>
        </dependency>

        <!-- =========================================== API documentation -->
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
            <version>${springdoc.version}</version>
        </dependency>

        <!-- ================================================== Monitoring -->
        <dependency>
            <groupId>io.micrometer</groupId>
            <artifactId>micrometer-registry-prometheus</artifactId>
        </dependency>

        <!-- =================================================== Utilities -->
        <dependency>
            <groupId>com.fasterxml.jackson.dataformat</groupId>
            <artifactId>jackson-dataformat-yaml</artifactId>
        </dependency>

        <!-- ===================================================== Testing -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <!-- Integration tests need a live MariaDB; unit tests never do. -->
                    <excludedGroups>integration</excludedGroups>
                    <!-- Mockito must be a startup agent on modern JDKs; dynamic self-attach
                         is deprecated and becomes an error in a future release. -->
                    <argLine>-javaagent:"${settings.localRepository}/org/mockito/mockito-core/${mockito.version}/mockito-core-${mockito.version}.jar"</argLine>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
